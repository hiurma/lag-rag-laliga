# LaLiga RAG SQL – MVP

Prototipo mínimo de RAG (SQL QA) con LangChain para consultar una base de datos local de LaLiga y generar análisis e informes.

## Estructura del proyecto
- `src/` → código Python  
- `data/` → mi base de datos `.sqlite` y descripciones de tablas  
- `reports/` → informes generados 

## Uso rápido (con Codespaces)
1. Crear un Codespace  
2. Añadir mi API key de OpenAI  
3. Subir mi base `laliga.sqlite` a `data/`  
4. Ejecutar:

